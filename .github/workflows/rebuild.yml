name: Rebuild Parent

on:
  workflow_dispatch: # Manual trigger
  repository_dispatch: # Triggered by child repo

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout parent
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Build/Deploy logic
        run: |
          echo "Rebuilding parent because child updated!"
          # Add build/deploy steps here
